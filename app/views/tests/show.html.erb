<h2>Тест '<%= @test.title %>'</h2>
<% if @test.questions.present? %>
<h3>Список вопросов:</h3>
  <table>
  <tr>
    <th>Содержание</th>
    <th colspan="3">Действия</th>
  </tr>
  <% @test.questions.each do |question| %>
    <tr>
      <td><%= question.body %></td>
      <td><%= link_to 'Просмотреть', question_path(question) %></td>
      <td><%= link_to 'Изменить', edit_question_path(question) %></td>
      <td><%= link_to 'Удалить', question_path(question),
                                  method: :delete,
                                  data: { confirm: 'Вы действительно хотите удалить запись?' } %></td>
    </tr>
  <% end %>
  </table>
<% else %>
  <p>Вопросов пока нет.</p>
<% end %>
<br>
<%= link_to 'Новый вопрос', new_test_question_path(@test) %>
<br><br>
<%= link_to 'Изменить', edit_test_path %> |
<%= link_to 'Вернуться', tests_path %>
<div id="test-progress">
  <%= tag.div @test_passage.progress(previous = true).to_s + "%", id: "test-bar", data: { test_passage_id: @test_passage.id, progress: @test_passage.progress, prev_progress: @test_passage.progress(previous = true) } %>
</div>
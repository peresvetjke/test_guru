<%= link_to 'Новый ответ', new_admin_question_answer_path(@question) %>

<h2>Вопрос '<%= @question.body %>'</h2>
<% if @question.answers.present? %>
<h3>Список ответов:</h3>
  <table class='table table-bordered'>
  <tr>
    <th>Содержание</th>
    <th>Верный</th>
    <th colspan="3">Действия</th>
  </tr>
  <% @question.answers.each do |answer| %>
    <tr>
      <td><%= answer.body %></td>
      <td><%= answer.correct ? "Да" : "Нет" %></td>
      <td><%= link_to 'Просмотреть', admin_answer_path(answer) %></td>
      <td><%= link_to 'Изменить', edit_admin_answer_path(answer) %></td>
      <td><%= link_to 'Удалить', admin_answer_path(answer),
                                  method: :delete,
                                  data: { confirm: 'Вы действительно хотите удалить запись?' } %></td>
    </tr>
  <% end %>
  </table>
<% else %>
  <p>Ответов пока нет.</p>
<% end %>

<p> <%= link_to 'Изменить', edit_admin_question_path(@question) %> |
<%= link_to 'Вернуться', admin_test_path(@question.test) %> </p>